/**
 * An object representing a column header and function to extract the row values from data objects.
 * @typedef {Object} Column
 * @example var column = {label: 'Example heading', value: function(d) { return d.value; }}
 */
